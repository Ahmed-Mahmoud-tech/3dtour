# Loaded by Vite only for `npm run build` (mode=production) — dev keeps the
# localhost:5173 fallbacks. Public URL, safe to commit; not dockerignored, so
# the docker image build picks it up too.
VITE_VIEWER_URL=https://gateverse.net
